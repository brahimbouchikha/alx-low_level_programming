#include <stdio.h>

/*
 * main - write aphabet in lowercase and uppercase
 * Return: 0
 */
int main(void)
{
	char lc,uc;

	lc = 'a';
	uc = 'A';
	while (a < 'z')
	{
		putchar(lc);
		lc++;
	}
	while (A < 'Z')
	{
		putchar(uc);
		uc++;
	}
	putchar('\n');
	return (0);
}
